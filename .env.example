# PORT=3000
# For production, set DATABASE_URL to use PostgreSQL
DATABASE_URL=postgres://<username>:<password>@localhost:5432/tasks_dev
# For local development without DATABASE_URL, in-memory repository is used
